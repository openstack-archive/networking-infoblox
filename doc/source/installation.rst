============
Installation
============

At the command line::

    $ pip install networking-infoblox

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv networking-infoblox
    $ pip install networking-infoblox